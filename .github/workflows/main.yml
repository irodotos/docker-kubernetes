name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Docker login
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" -p "${{ secrets.DOCKER_PASSWORD }}"

    - name: Build docker image
      run: docker build . --file Dockerfile --tag ${{ secrets.DOCKER_USERNAME }}/nginx-hy548:$(date +%s)

    - name: Push docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/nginx-hy548